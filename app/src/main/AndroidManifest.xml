<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.casa.myapplication">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Model.LoginActivity"
            android:windowSoftInputMode="stateHidden"
            android:noHistory="true">
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Model.MenuActivity"
            android:windowSoftInputMode="stateHidden"
            android:label="@string/app_menu" />
        <activity
            android:name=".Model.NewOrderActivity"
            android:windowSoftInputMode="stateHidden"
            android:label="@string/app_new_order" />
        <activity
            android:name=".Model.WatchOrdersActivity"
            android:windowSoftInputMode="stateHidden"
            android:label="@string/app_watch_order" />
        <activity
            android:name=".Model.SettingsActivity"
            android:windowSoftInputMode="stateHidden"
            android:label="@string/app_settings" />
        <activity
            android:name=".Model.ChangeSettingsActivity"
            android:windowSoftInputMode="stateHidden"
            android:label="@string/app_change_settings" />
        <activity
            android:name=".Model.PetrolActivity"
            android:windowSoftInputMode="stateHidden"
            android:label="@string/app_petrol" />
        <activity
            android:name=".Model.MapsActivity"
            android:windowSoftInputMode="stateHidden"
            android:label="@string/app_maps" />
        <activity
            android:name=".Model.AddClientActivity"
            android:windowSoftInputMode="stateHidden"
            android:label="@string/app_add_client" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>

</manifest>